import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  buttonGroup: {
    display: "flex",
    justifyContent: "space-between",
    padding: "1rem 0.5rem",
  },
  button: {
    width: "33%",
    maxWidth: 140,
  },
}));
